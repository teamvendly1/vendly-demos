# INTERNAL BUILD ARTEFACTS - NEVER PUBLISH. Added 2026-08-09.
#
# Measured today: 533 published demos exposed HARVEST.json and BUILD-REPORT.json at their own
# public demo URL. Anyone we emailed a link to could fetch our internal notes about their own
# business. On mike-s-auto-glass-express the harvest read "Nations Auto Glass is a growing
# multi-state company" - our evidence that we had scraped a competitor - readable at the URL we
# were about to send them. 105 of the first 120 demos carried an internal note or another
# client name in a publicly fetchable file.
#
# DEMO-VISUAL-FAIL.txt was the worst of them: our own note saying the demo failed, served from
# the prospect-facing URL.
#
# Every one of these lives in the build tree at _tmp/autodemo/demos/<slug>/, which is where
# tooling should read them. Nothing is lost by keeping them out of the public repo.
HARVEST.json
BUILD-REPORT.json
BUILD-REFUSED.json
FILL.json
ASSET-LEDGER.json
STOCK-PROVENANCE.json
PHOTO-TEXT-SCORE.json
FOOD-BREADTH.json
DEMO-VISUAL-PASS.txt
DEMO-VISUAL-FAIL.txt
PIXEL-AUDIT-PASS.txt
PIXEL-AUDIT-FAIL.txt
DEMO-RESEARCH.md
SOURCE.txt

# ---- MINORS UNPUBLISH 2026-09-01 (NO-MINORS-IN-ANY-DEMO-OR-CONTENT-FOREVER) ----
# Jacob, 2026-09-01, absolute: "get kids off the damn screen ... under no circumstance."
# Uncertain is a REFUSAL. These 233 demos each serve at least one HARVESTED
# photograph and NOBODY HAS EVER OPENED A SINGLE FRAME on any of them. Five demos taken down
# earlier today were found by looking, so the rate when we actually look is not zero.
#
# They are UNPUBLISHED, not deleted - every file is still on disk at this path. Each one comes
# back the moment a named reviewer opens every photo asset and records a CLEAR in
# _remote/state/demo-minors-clearance.jsonl: delete its line here, git add, commit.
#
# This list is here rather than only in a commit because the demo factory is live in this repo
# and `git add -A` would otherwise republish an untracked directory within the hour.

/hallstreeservices/
/streatertowingconcordcabarrusnc/
/ultimatetowing/
/meandeansautomotive/
/mytreeboyzllc/
/rjautomotive/
/snaganddragtowingllc/
/johnnystowingandrecovery/
/loftinsautomotivehotrodsandcustomsgastoniagastonnc/
/westernauto/
/carloscompletecarcarecenter/
/santeequicklube/
/elite-tree-services/
/completeautotruckrepairelizabethunionnj/

# ---- BUILD STAGING DIRECTORIES - NEVER PUBLISH. Added 2026-09-01 (seat minors-unpublish-242). ----
# generate-from-blank-mold stages a rebuild in <slug>.stage-<pid>/ and renames it over <slug>/ only
# on success. 37 of these were sitting in the working tree at 14:22 today, the oldest 78 minutes,
# because a failed or in-flight rebuild never cleans them up. publish-site-demos.ps1 line 494 runs
# , which would commit every one of them and serve it at /<slug>.stage-<pid>/.
# It has never happened (0 in HEAD) - that is timing, not a guard.
#
# It matters beyond tidiness: 15 of those 37 are demos unpublished today under the minors law, so
# one unlucky  would have republished an unexamined page at a sibling URL and made the
# takedown look complete while it was not.
*.stage-*/
/a1autorepair/
/parkerselectricllcdunnharnettnc/
